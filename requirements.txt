# Base
python-dotenv>=1.0
pillow>=10.0
tqdm>=4.66
jiwer>=3.0
gradio>=5.0  # UI (scripts/ui.py) + label editor (scripts/labeling)
pymupdf>=1.24  # render trang PDF scan nhiều trang (pip thuần, không cần system lib)
python-docx>=1.1  # trich anh nhung tu file Word .docx de OCR

# Deep learning (KHÔNG liệt kê torch/torchvision ở đây: PyPI mặc định là bản CPU.
# Các script run_train.* tự cài bản CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128)
transformers>=4.51
accelerate>=1.1
peft>=0.14
trl>=0.14
bitsandbytes>=0.45
# peft crash ImportError khi gan LoRA adapter neu dinh torchao cu (Colab cai san 0.10.0,
# yeu cau >0.16.0). Windows bo qua (khong co wheel / khong can: thieu thi peft tu bo qua).
torchao>=0.16; sys_platform != "win32"
datasets>=3.0
sentencepiece>=0.2.0
tiktoken>=0.8
# Hậu xử lý tiếng Việt: chuẩn hóa NFC/old-diacritics/Đ-Ð (thư viện NLP tiếng Việt nổi tiếng)
underthesea>=9.0
# Tìm kiếm vector gần đúng cho retrieval few-shot (tùy chọn; thiếu thì fallback numpy)
faiss-cpu>=1.8; python_version < "3.14"

# Optional: chỉ cài trên Linux/WSL, tăng tốc attention
# flash-attn>=2.6